CREATE TABLE employees (
    id INT PRIMARY KEY,
    name VARCHAR(50),
    gender VARCHAR(10),
    age INT,
    department VARCHAR(50),
    salary DECIMAL(10,2),
    experience_years INT,
    joining_date DATE,
    attrition VARCHAR(10)
);
